# The *first* value set is the one that's used, so defaults must be the last
# options in the file.
ServerAliveInterval 30
TCPKeepAlive yes
ConnectTimeout 30
ControlMaster auto
ControlPath ~/tmp/%u-ssh-socket-%r@%h:%p

